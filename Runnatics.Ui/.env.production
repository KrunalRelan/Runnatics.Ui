VITE_API_BASE_URL=https://runnatics-api-hwahc8ckgeedefdc.centralindia-01.azurewebsites.net/api
# VITE_ENCRYPTION_KEY is intentionally omitted here — it is a secret.
# Set it in Azure Portal → Static Web Apps → Environment variables.
# For local production testing, put it in .env.production.local (gitignored).