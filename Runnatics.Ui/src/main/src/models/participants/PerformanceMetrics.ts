// Performance metrics data structure

export interface PerformanceMetrics {
  averageSpeed: number;
  averagePace: string;
  maxSpeed: number;
  minPace: string;
  overallRank: number;
  genderRank: number;
  categoryRank: number;
  overallCategoryRank: number;
  totalParticipants: number;
  genderParticipants: number;
  categoryParticipants: number;
}
